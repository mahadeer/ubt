<project default="run" basedir="ant">
    <property name="authorname" value="mahadeer"/>
    <target name="run">
        <property name="authorfile" value="sample"/>
        <echo>Hello World by UBS Tools by ${authorname}</echo>
        <mkdir dir="bin/${authorname}" />
        <mkdir dir="del" />
        <touch file="bin/${authorname}/${authorfile}.txt" />
        <delete dir="del" />
    </target>
</project>